import { GetPlaceDirective } from './get-place.directive';

describe('GetPlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new GetPlaceDirective();
    expect(directive).toBeTruthy();
  });
});
